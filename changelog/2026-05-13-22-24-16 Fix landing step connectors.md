# Fix landing step connectors

Adjusted the landing page "How it works" connector lines so they span from each step number toward the next step instead of appearing as short disconnected dashes in the column gaps.
